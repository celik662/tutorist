<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:paddingHorizontal="16dp"
    android:paddingTop="10dp"
    android:paddingBottom="4dp"
    android:orientation="vertical"
    android:background="@android:color/white"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- Drag handle -->
    <View
        android:layout_width="36dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:background="@color/tutorist_surfaceVariant"
        android:alpha="0.6"
        android:layout_marginBottom="12dp"
        android:layout_marginTop="4dp"
        android:clipToOutline="true" />

    <TextView
        android:text="Yaklaşan Dersler"
        android:textStyle="bold"
        android:textSize="18sp"
        android:textColor="@color/tutorist_onSurface"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        android:layout_marginTop="2dp"
        android:text="Onaylı ve henüz başlamamış dersler"
        android:textSize="12sp"
        android:textColor="@color/tutorist_onSurfaceVariant"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvUpcoming"
        android:layout_marginTop="12dp"
        android:overScrollMode="never"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
</LinearLayout>
